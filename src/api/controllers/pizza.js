import PizzaService from "../services/pizza";
const pizzaService = new OrdersService();
const headers = {
  'Content-Type': 'application/json'
};

module.exports = (req, res) => {
  getAllPizza: async(req, res) => {
    try {
      
    } catch (error) {
      
    }
  }
}